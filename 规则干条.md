# 规则干条

> **范围**：仅本规范仓维护者速查，**不**复制到业务项目，**不**参与接入/升级。细则与例外以 `AGENTS.md`、`docs/` 与工作流原文为准；冲突时按「用户已确认需求 → 唯一需求载体 → 项目规范 → 工具/skill 默认」处理。

## 一、工具与规范载体

1. 默认假定 **OpenSpec、CodeGraph、Superpowers** 均已安装并可调用；**需要使用**某项却发现未安装、未初始化或当前会话不可用时，立即中断并明确告知是哪一个工具，不得伪装可用、静默降级或擅自安装。不依赖该工具的任务不受此阻塞。
2. **`AGENTS.md` 未经用户明确要求不得修改**；只保留 AI 每次 Chat 都需要知道、在读取索引前必须生效，以及用于判断需求授权和按需路由的规则。
3. 项目长期知识与条件化流程放 `docs/`，以 `docs/README.md` 为索引并按需读取；不得把 AI 每次 Chat 都需要知道的规则只写在 `docs/`。新增、移动、改名或删除规范时必须同步维护索引。
4. OpenSpec 只由用户明确确认的新增需求或需求变更触发；用户未确认需求意图和具体载体前，不得创建、选定或更新 change。确认后，同一需求后续工作只用该 **OpenSpec change** 作为唯一载体；issue/PR/任务卡等只作输入来源，不得替代载体或维护平行口径。
5. 项目级 skill 放 `.agents/skills/`，先维护 `.agents/README.md` 索引；个人全局 skill 不属于项目规范。
6. 需要项目特有知识或 `AGENTS.md` 明确触发时：先读 `docs/README.md`，再只加载直接相关文档；不得在每个 Chat 中默认加载 `docs/`，不得猜测缺失内容或为扩上下文批量读取。
7. Superpowers 只作方法，不提供创建、选定或更新需求载体的用户授权。用户确认进入 OpenSpec 流程后，change 优先于 skill 默认落盘与提交流程；不得另建 `docs/superpowers/` 等平行文档，不得因 skill 默认自动提交；用户要求跳过某类方法 skill 时从其要求。

## 二、六条底线（始终生效）

8. **不伪造成功**：真实失败必须显式暴露，不用默认值、空对象、mock、旧链路或跳过检查掩盖。隔离 mock 仅可用于已标明边界的测试、演示或约定的 UI 阶段 1，不得冒充真实业务依赖或掩盖失败。
9. **完成前必须验证**：必须说明实际结果；未完成必要验证时不得声称完成或通过，并必须写明原因、未覆盖项和剩余风险。
10. **改行为前先记录**：用户确认新增或变更需求后，改变外部行为、接口、数据、权限、流程或验收前，必须先更新用户确认的唯一需求载体；未更新前不得实施。
11. **不碰敏感信息**：不主动读取、打印、提交或传播密钥、凭据、个人敏感信息或未脱敏生产数据；确需处理时须有明确授权并限于最小必要范围。
12. **不擅自制造外部副作用**：未经用户明确要求，不 commit / push / merge / release / deploy / 归档需求记录，不删除、迁移、写入生产数据或执行不可逆、付费、对外操作。
13. **拿不准就问**：AI 判断内容可能构成新增需求或需求变更时，必须先说明依据和影响，请求用户确认是否作为正式需求处理；需求意图确认前不得查找 OpenSpec 候选或进入 OpenSpec 流程。

## 三、需求与跨 Chat

14. 用户及已确认记录决定需求事实；源码、测试、版本历史、行为与代码分析只证明代码事实，不能单独定义需求或验收。
15. 用户确认需求意图后，才可只读核对直接相关记录；即使只有一个候选 change，也必须展示其状态、关系与建议，由用户确认「使用原 change 还是新建关联 change」。没有候选时，先澄清并确认目标、范围和验收口径，再提出拟用 ID；用户确认具体载体前不创建、不选定、不更新 change，也不形成最终设计、计划或改代码。
16. 跨 Chat 或上下文压缩后，不依赖对话记忆自动继续；用户明确 `change-id` 后才重新读取对应 OpenSpec change（proposal / specs / design / tasks；变更还要读变化记录）。未明确时只读查找并展示候选，取得确认后才能恢复。
17. 用户未明确确认需求意图和具体载体前，不创建 change、不实现、不形成最终设计与计划；已确认结论须在当前 Chat 先写入用户确认的载体后再切换。
18. 需求变更：先定位原记录 → 判断变化等级 → 正文改为当前口径 → **追加**变化记录（不覆盖历史）→ 再改代码；已归档 change 原则上新开 change 承接。

## 四、实施边界

19. 修改前区分「目标变更」与「非目标不变式」；未要求改变的行为、接口、数据、权限、错误处理、性能、布局、调用方式与兼容性一律视为不变式。
20. 只改实现目标所必需的内容；不得靠放宽校验、吞错、默认成功、绕过权限、扩大默认权限、削弱类型或破坏兼容性制造局部效果。
21. 必须改非目标不变式时，先说明原因、影响与验证方式，等用户确认后再实施。
22. 保护用户已有工作：不得擅自覆盖、回退、删除或格式化已有改动；无法安全合并时停止并说明。
23. 用最少代码解决问题；不为一次性或假想需求加抽象与配置；不顺手重构或清理无关死代码。
24. 引入依赖、服务、技术栈或绕过既有依赖前，说明原因、影响和验证成本。
25. 源码、配置和文档默认 UTF-8 无 BOM；中文项目新增内容默认简体中文，写入后确认无乱码。

## 五、正确性与安全

26. 生产与真实业务链路不得用 mock、本地缓存、seed、旧字段或旧链路冒充真实依赖；测试/演示/本地模拟必须隔离并标明边界。
27. 只有业务上确实无数据时才能返回空；不可读、不可解析、契约缺失、接口失败或权限失败都必须当错误处理。
28. 面向用户的错误信息不得泄露内部敏感细节；疑似泄露时停止扩散，只说明风险和位置，不复制敏感内容。
29. 不把 `.env`、凭据、连接串或令牌写入源码、文档、示例、测试快照、日志或提交说明；不伪造权限、凭据或安全校验结果。
30. 可能重试、跨系统或影响真实业务的关键写操作须幂等（或等效防重），确认写入结果后再继续依赖该结果的流程。

## 六、验证与交付

31. 验证覆盖本次改动及直接影响；结构/交互/校验/服务/权限/数据契约/外部集成须扩到直接调用方与关键反向路径。
32. 不得通过跳过、删除或弱化测试，缩小必要验证范围，或只改断言/期望值来制造通过结果。
33. 完成前重新运行能证明结论的验证并读完整结果；不得把部分验证外推为整体通过。
34. 完成后说明：实际改变、已核对的直接相关不变式、验证证据、未验证项和剩余风险。
35. 用户要求提交/发布前，先汇总变更、关联需求、验证结果和剩余风险；验证不完整或高风险必须明确披露。
36. 提交信息无约定时默认简体中文，标题描述实际结果，避免空泛措辞，并按需注明需求记录。

## 七、代码设计（取舍）

37. 先保证行为正确、错误显式、契约清楚，再追求简单直接。
38. **KISS**：能局部、用现有模式解决的，不新建平行架构或多余抽象。
39. **YAGNI**：不为假想需求提前做扩展点、配置层、通用框架。
40. **DRY**：合并的是同一知识/业务规则，不是「长得像」；语义不同可保留独立实现。
41. 关注点分离、高内聚低耦合；命名与默认行为符合最小惊讶，默认值不得掩盖失败。

## 八、工具协作顺序

42. AI 识别潜在新增/变更需求 → 说明依据与影响 → 用户确认需求意图 → 只读核对并展示候选，或在需求口径确认后提出新 ID → 用户确认具体 OpenSpec change → 再执行写入及后续工具流程（OpenSpec 承载需求；CodeGraph 按需查代码事实；Superpowers 提供方法）→ 工具输出与源码/测试/命令结果核对 → 回填该 change。细则见 `docs/规范/工具协作.md`「Superpowers 与载体优先级」。
43. CodeGraph 不定义需求、不要求每步点名；OpenSpec 不根据源码猜需求；工具已安装不等于已获提交/发布/写生产授权。
44. 斜杠命令等只按当前 AI 工具实际提供的形式使用；需要用户重启才能加载新能力时说明后停止，不得假装已加载。

## 九、UI 两阶段（仅适用时）

45. 仅在明确采用规定标题「阶段 1：UI、隔离 mock 与页面跳转；阶段 2：真实接口、数据、权限、状态流转与错误处理」或已有对应重组标记时，启用 `docs/规范/工具协作.md` §5。
46. 阶段 1 禁止接入真实接口、真实数据、权限判断或业务状态流转；须人工 UI 验收通过后再做阶段 2。
47. UI 权威来源必须可读取、非占位符；不得擅自把仓库目录、旧稿、缓存或历史附件加入权威集合。
48. 首次落盘任务只依据已确认文字需求；再读 UI；读 UI 前须固化基线/快照，不得覆盖已有有效基线。
49. 重组后必须通过机械核对（checkbox、编号、映射、原子约束、职责）方可声明无遗漏或开始实现；任一检查失败则停止。

## 十、内容归属速记

| 放这里 | 放什么 |
| --- | --- |
| `AGENTS.md` | 每次 Chat 常驻的跨项目底线、需求确认与按需路由规则（改前须用户明确要求） |
| `docs/` | 长期、跨需求项目规范 |
| OpenSpec change（唯一） | 用户明确确认的进行中需求及其目标、范围、验收、设计、任务、变化记录 |
| `.agents/` | 随仓库共享的项目 skill |
| `design/` | UI/交互/视觉资料（接入时不得改 README 以外的设计文件） |
| `规则干条.md` | 仅本规范仓速查（不接入业务项目） |
| `新增需求工作流.md` / `需求变更工作流.md` | 仅本规范仓工作流教程（不接入业务项目） |
