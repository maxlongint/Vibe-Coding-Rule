# 项目规范文档（docs/）

本目录存放**长期有效**的项目规范：技术栈约定、架构说明、跨需求业务规则、环境配置说明等。

## 与需求变更文档的区别

| 载体 | 路径 | 放什么 | 何时用 |
| --- | --- | --- | --- |
| **项目规范** | `docs/`（本目录） | 相对稳定、跨需求有效的约定 | 团队日常开发查阅 |
| **需求变更** | `openspec/changes/<change-name>/` | 进行中的 proposal、spec、design、tasks、接口/API 契约变化 | 启用 OpenSpec 时 |
| **需求变更（轻量）** | issue / PR 描述 / 任务卡 | 同上，无 OpenSpec 时的等价载体 | 未启用 OpenSpec 时 |

不要把「某次需求的 proposal / tasks」写进 `docs/`；也不要把「长期架构约定」只写在某个 change 里而不沉淀到 `docs/`（确需沉淀时，验收后合并进本目录）。

## 建议组织方式

按项目实际需要分子目录，例如：

```text
docs/
├── README.md           # 本文件：docs 目录说明与索引
├── experience/         # 项目结束、阶段复盘、问题沉淀后的经验包
├── 架构/               # 架构说明、模块关系、关键设计决策
└── 规范/               # 编码、协作等团队规范
```

## 经验包

`docs/experience/` 用于存放项目结束、阶段复盘或问题沉淀后形成的经验包 Markdown。经验包不是 skill，不放入 `.agents/skills/`；如后续需要被 AI 按固定流程调用，再提炼为项目级 skill。

新增经验包时，应同步在本节或下方索引中添加入口，便于后续查找和引用。

## 索引（按需维护）

| 文档 | 说明 |
| --- | --- |
| [规范/代码设计原则.md](规范/代码设计原则.md) | 通用代码设计原则：KISS、YAGNI、DRY、关注点分离、高内聚低耦合、最小惊讶原则 |
