# AGENTS.md

> Vibe Coding Rule 版本：v4.0.0

## 速查：六条底线

1. **不伪造成功**：真实失败必须显式暴露，不用默认值、空对象、mock、旧链路或跳过检查掩盖。隔离 mock 仅可用于已标明边界的测试或演示，不得冒充真实业务依赖。
2. **完成前必须验证**：必须说明实际结果；未完成必要验证时不得声称完成或通过，并写明原因、未覆盖项和剩余风险。
3. **改行为前先记录**：用户明确确认新增或变更需求后，改变外部行为、接口、数据、权限、流程或验收前，必须先更新用户确认的唯一需求载体。
4. **不碰敏感信息**：不主动读取、打印、提交或传播密钥、凭据、个人敏感信息或未脱敏生产数据；确需处理时须有明确授权并限于最小必要范围。
5. **不擅自制造外部副作用**：未经用户明确要求，不提交、发布、删除、迁移、写入生产数据或执行其他不可逆、付费或对外操作。
6. **拿不准就问**：用户已指定的需求承接方向与已确认口径冲突，或用户要求判断承接关系但边界不清时，必须停止实施，说明事实、影响和未知项，并请求用户决定。

## 1. 适用范围与优先级

- 外部指令优先级遵循当前运行环境；本文件不能覆盖平台、法律、权限安全约束或用户本轮明确指令。
- 项目内部冲突按“用户已确认的需求事实 → 当前唯一需求载体 → 适用的项目规范 → 工具输出与运行环境”处理；仍无法判断时停止并询问。
- 用户及已确认记录决定需求事实；源码、测试、版本历史、现有行为和代码分析结果只证明代码事实，不能单独定义需求或验收。
- 多级目录存在多个 `AGENTS.md` 时，优先遵守离目标文件最近且适用的项目规则；无法确定作用域时先确认。
- 修改本规范时，现有条文是审阅对象，不得把它当作阻止修订的前置门禁；敏感信息、权限安全、外部副作用和完成前验证约束仍然有效。

## 2. 内容归属与上下文

- **`AGENTS.md`**：AI 常驻规则，保存每次 Chat 都必须知道、读取索引前必须生效、用于判断是否读取 `docs/` 或是否允许继续的规则；未经用户明确要求不得修改。
- **`docs/`**：长期有效、跨需求适用且按需读取的项目知识，以 `docs/README.md` 为索引；新增、调整、移动或删除规范时必须同步维护索引。
- **OpenSpec change**：用户已明确确认的新增需求或需求变更，以及同一需求后续目标、范围、验收、设计、任务、实现记录、验证结果和归档历史。issue、PR、任务卡、设计图与外部文档只作输入来源。
- **`.agents/`**：随仓库共享的项目级 skill；先维护 `.agents/README.md` 索引，再按需创建 `.agents/skills/<skill-name>/SKILL.md`。
- 用户提供的经验、素材、偏好或讨论结论，除非明确要求保存并指定或确认归属，否则不得写入仓库文件或 OpenSpec；归属不明确时先询问。
- 当前任务需要项目特有知识，或本文件明确要求读取项目规范时，先读 `docs/README.md`，再只加载直接相关文档；不得为扩大上下文而批量读取。
- `docs/` 不承载 OpenSpec 与 Serena 的流程说明；相关常驻路由与边界以本文件为准。

## 3. 需求确认与 OpenSpec

- 普通问答、解释、只读分析、代码理解、仓库状态检查、未确认的需求讨论，以及不改变需求口径的内部整理，默认不使用 OpenSpec。本规范自身的审阅和修订按 §1 的例外处理。
- 新增需求、需求变更或已有需求的继续工作，只能由用户明确指定。AI 不得自行分类、要求确认、查找或启动 OpenSpec；用户未指定时，按其直接授权的任务处理。
- 只有在用户已指定承接方向且该方向与已确认口径冲突，或用户要求判断承接关系但边界不清时，AI 才说明事实、依据、影响和未知项，并请求用户决定；AI 的判断不构成选定或创建需求载体的授权。

### 3.1 新增需求

流程：

1. 用户明确指定新增需求后，AI 可以澄清目标、范围和验收口径。
2. 用户确认口径并主动要求创建 OpenSpec change 后，AI 生成简短明确的拟用 `change-id`。
3. AI 只读检查现有 change；发现同名或可能承载该需求的 change 时停止，列出冲突项、状态和关系，不改用其他 ID。
4. 无冲突时使用生成的 ID 创建 change，无需用户另行确认该 ID。

创建授权与冲突检查完成前，不得创建、选定或更新 OpenSpec change，不得形成正式需求、最终设计、计划或开始实施。

### 3.2 需求变更或已有需求后续

流程：

1. 用户明确指定需求变更或已有需求后续工作后，AI 只能继续澄清并只读核对直接相关记录。
2. 用户已明确当前 `change-id` 时继续使用该 change；未明确时，展示全部候选 change、状态、与本次请求的关系及建议，并等待确认。
3. 即使只有一个明显候选，也不得擅自选定；多个候选或证据冲突时停止并请求选择。
4. 继续原 change 时，先确认载体；新建关联 change 时，先确认新口径，再由用户主动要求创建并执行同等冲突检查。

载体确认或新建授权完成前，不得写入 change、修改代码或形成冲突口径；完成后先写入当前唯一 change，再实施会改变外部行为、接口、数据、权限、流程或验收的内容。

## 4. Serena 与代码事实

- 默认假定 OpenSpec 与 Serena 均已安装并完成当前项目和当前 AI 工具侧接入；只在当前任务确实需要时检查可用性。需要使用却不可用时，中断相关流程并指出工具和阻塞点；不得伪装可用、静默降级或擅自安装。
- OpenSpec 是规格与变更制品层，不根据源码猜需求，不替代代码语义分析、测试或用户确认。
- Serena 是代码语义工具层，不定义需求事实，不替代 OpenSpec、当前源码、diff、测试或完成前验证。
- 需要理解代码结构、符号关系、引用、调用影响、诊断或语义编辑时，优先使用 Serena 取证或操作；Serena 输出仍须与当前源码、diff、诊断、测试或实际命令结果核对。
- Serena memory、shell 或编辑能力不得作为需求载体、长期项目规范或验证结论，不得绕过用户授权、敏感信息、外部副作用、工作区保护和完成前验证。
- 跨 Chat 或上下文压缩后继续已有需求时，用户必须明确当前 `change-id`，或确认 AI 只读查找后列出的候选；确认前不得自动恢复、选定或修改 change。
- 跨 Chat 或上下文压缩后继续代码实现、验证或影响分析时，必须重新确认 Serena 当前项目上下文并取得当轮证据；不得依赖未落盘记忆或过期工具输出。

## 5. 实施边界

- 改变外部可观察行为、接口契约、数据结构、权限、状态流转、验收标准或用户路径前，必须先更新用户确认的唯一需求载体。
- 修改前必须区分**目标变更**与**非目标不变式**；未要求改变的行为、接口、数据、权限、错误处理、性能、布局、调用方式和兼容性默认不变。
- 只改变实现目标所必需的内容，不通过放宽校验、吞错、默认成功、绕过权限、扩大默认权限、削弱类型约束或破坏兼容性制造局部效果。
- 必须改变非目标不变式时，先说明原因、影响范围和验证方式；未经用户确认不得实施。
- 修改前识别工作区和目标文件中的已有改动，包括相关、未提交或与目标行重叠的内容；不得擅自覆盖、回退、删除或格式化。无法安全合并时停止并说明。
- 用最少代码解决当前问题，不为一次性或假想需求增加抽象与配置，不顺手重构或清理既有死代码。
- 注释解释意图、边界和非显然副作用，不复述代码；临时方案或技术债务用 `TODO:` / `FIXME:` 标明背景和影响。
- 源码、配置和文档默认 UTF-8 无 BOM；中文项目新增内容默认简体中文，写入后读取确认无乱码或错误转码。

## 6. 正确性、安全与验证

- 优先保证业务正确性、边界清晰和可测试性；遵循项目既有架构、目录、命名与调用方式。引入依赖、服务、技术栈或绕过既有依赖前，说明原因、影响和验证成本。
- 生产和真实业务链路不得用 mock、本地缓存、seed、旧字段或旧链路冒充真实依赖；测试、演示和本地开发的模拟数据必须隔离并标明边界。
- 只有业务上确实无数据时才能返回空；不可读、不可解析、契约缺失、接口失败或权限失败都属于错误，不得用默认值、猜测值、空对象或默认成功掩盖。
- 展示日志、报错、配置或命令输出前先检查敏感信息；发现疑似泄露时停止扩散，只说明风险和位置，不复制敏感内容。
- 不把 `.env`、凭据、连接串或令牌写入源码、文档、示例、测试快照、日志或提交说明；不伪造权限、凭据或安全校验结果。
- 可能重试、跨系统或影响真实业务的关键写操作必须采用幂等或等效防重机制，并确认写入结果。
- 验证必须覆盖本次改动及直接影响；涉及结构、交互、输入校验、服务、权限、数据契约或外部集成时，扩展到直接调用方和关键反向路径。纯文案或局部呈现可以做最小验证。
- 优先使用项目已有的测试、类型检查、lint、构建或手工验收命令；项目固定运行时或工具链版本时先按约定切换。
- 不得通过跳过、删除或弱化测试，缩小必要验证范围，或仅修改断言和期望值来制造通过结果。
- 完成前重新运行能够证明结论的验证，读取完整结果；失败、无法执行或覆盖不足时，说明原因、影响、未覆盖项和剩余风险。

## 7. 外部副作用与交付

- 删除、覆盖、数据迁移、生产写入、权限变更、外部通信、付费操作或其他难以恢复的动作前，确认准确目标、影响范围、授权和可恢复方式；任一项不清楚时不得执行。
- 未经用户明确要求，不得 commit、push、merge、release、deploy、归档需求记录或执行其他改变仓库历史、远端及线上状态的操作。
- 用户要求提交或发布前，先汇总当前变更、关联需求、验证结果和剩余风险；验证不完整或存在高风险时必须明确披露。
- 提交信息遵循项目约定；无约定时默认使用简体中文，标题描述实际结果，避免“修改”“更新”“fix”等空泛措辞，并按需注明需求记录。
